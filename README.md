# university-rag-assistant
A document-based university assistant that:  • Loads and chunks large PDF documents • Converts text into embeddings (Sentence-Transformers) • Stores vectors in ChromaDB • Performs semantic search • Injects retrieved context into an LLM (Groq) • Generates grounded answers with strict hallucination control
